# 项目收尾清单

当前阶段先完成发布前收尾，不进入真实部署。

## 已收尾

- 已从 `D:\Desktop\Work\CV` 提取可确认的简历信息。
- 已替换占位邮箱为 `2922188469@qq.com`。
- 已将定位更新为“全栈开发工程师 / 桌面端工具链工程师”。
- 已补齐 5 个真实项目方向：
  - 桌面端软件打包与发布工具链
  - 官网与下载发布系统
  - 用户管理与订阅支付系统
  - Python 自动化运营与数据工具
  - 技术支持 AI 与运维监控
- 已新增小程序内打印版页面：`pages/print/print`。
- 已在联系面板增加“打印版”入口。
- 已保留云开发为后续部署阶段，不在收尾阶段开启。

## 仍需人工补充

- 头像图片：当前 `profile.avatar` 为空。
- 微信二维码：当前 `profile.contact.wechatQr` 为空。
- 云开发环境 ID：当前 `config/env.js` 中 `cloud.envId` 为空。
- 订阅消息模板 ID：当前 `subscription.projectBrowseTemplateId` 为空。
- 管理员 openid 白名单：当前 `admin.allowedOpenIds` 为空。
- 真机预览与微信开发者工具上传前检查。

## 验收建议

1. 在微信开发者工具中打开项目。
2. 检查首页、项目详情、海报、打印版、反馈和数据看板页面。
3. 连续点击头像 5 次，确认数据看板可进入。
4. 在联系面板点击“打印版”，确认页面内容完整且没有文字溢出。
5. 安装 Node 后运行：

```bash
npm test
```
